{
  "scripts": {
    "build": "rm -rf dist && mkdir dist && npm run build-js && npm run build-css",
    "build-js": "uglifyjs js/*.js > layout.min.js",
    "build-css": "cat css/*.css | csso > layout.min.css"
  },
  "dependencies": {
    "csso-cli": "^1.0.0",
    "uglify-js": "^3.0.13"
  }
}
